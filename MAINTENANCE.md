# 🔧 小黑米排休系統 — 維護指南

> 本文件供未來維護者（含 AI 模型如 Claude Opus）快速上手。
> 系統架構：React (Vite) + Netlify Functions + Neon PostgreSQL

---

## 專案結構

```
dayoff-scheduler/
├── src/
│   ├── App.jsx          ← 主程式（UI + 業務邏輯，單一檔案）
│   ├── holidays.js      ← ⭐ 行事曆資料（每年維護一次）
│   ├── api.js           ← 前端 API 呼叫（很少需要動）
│   └── main.jsx         ← React 進入點（不需動）
├── netlify/functions/
│   └── db.mjs           ← Serverless API，連接 Neon DB
├── netlify.toml         ← 部署設定
├── MAINTENANCE.md       ← 本文件
└── README.md            ← 部署教學
```

---

## 📅 每年一次：更新行事曆（最重要）

**時機**：行政院人事行政總處公告次年行事曆時（通常每年 5-6 月）
**資料來源**：https://www.dgpa.gov.tw

**只需修改 `src/holidays.js`，兩個步驟：**

1. 在 `H_YEAR` 新增次年物件，參考 2026 年的格式：
   - 農曆節日（除夕、春節、端午、中秋）每年日期不同
   - 所有補假日都要列入（假日逢週六→前一工作日補假；逢週日→下一工作日補假）
   - 2026 年起無補班制度
2. 更新 `SCHEDULE_END` 到新年度，例如 `{ year: 2027, month: 11 }`
   - 這會自動延伸前端的月份選單

**驗證**：`npm run build` 通過即可。也可呼叫 `selfCheck(2027)` 檢查資料格式。

---

## 📆 每月例行：開啟新月份排休

**不需要改程式碼**，管理員在網站上操作即可：

1. 開啟網站 → 管理後台（密碼 `888888`）
2. 點「🔄 全部重置（新月份）」→ 確認
   - 會清空：排休、兼職時段、公休日、禁休日、特休、不需排休
   - 會保留：人員名單、排休限制設定
3. 系統自動偵測當下日期，預設顯示下個月

**排休開放開關**：管理後台最上方有「🔓 排休開放」開關，控制當月是否讓員工填寫。
- 關閉時：員工僅能查看，無法點選（避免提前畫休）
- 開放時：員工才能排休/排班
- 每個月獨立控制，儲存在 `config.openMonths`（key 為 `年-月索引`，如 `"2026-6"` = 2026年7月）
- 全部重置**不會**影響開放狀態

> ⚠️ 注意：目前所有月份共用一份公休日/禁休日設定，重置後需重新設定當月的公休日。
> 排休紀錄、特休、不需排休則是**依月份獨立儲存**，不會互相影響。

---

## 🔑 重要業務規則（改動前必讀）

| 規則 | 值 | 位置 |
|------|-----|------|
| 平日排休上限 | 2 人/天 | 管理後台可調（存 DB） |
| 週末排休上限 | 1 人/天 | 同上 |
| 國定假日排休上限 | 1 人/天 | 同上 |
| 每月排休總上限 | 5 天/人 | 同上 |
| 每月假日排休上限 | 2 天/人（含週末+國定假日） | 同上 |
| 特休 | 純意願登記（1-5天），不影響排休限制 | `specialIntent` |
| 兼職 | 無人數限制，時段：全天/中午/晚上（全天與其他互斥） | `ptSlots` |
| 管理後台密碼 | `888888` | App.jsx 內搜尋 `888888` |

## 💾 資料結構（Neon DB `app_data` 表，key-value JSONB）

| key | 結構 | 月份獨立？ |
|-----|------|-----------|
| `config` | `{maxWeekday, maxWeekend, maxHoliday, maxPerMonth, maxHolidayMonth, openMonths}` | 否（全域） |
| `employees` | `["王小明", ...]` | 否 |
| `partTimeEmployees` | `["陳小瑜", ...]` | 否 |
| `leaves` | `{"王小明": ["2026-07-01", ...]}` | 是（日期本身含月份） |
| `ptSlots` | `{"陳小瑜": {"2026-07-01": ["noon"]}}` | 是 |
| `specialIntent` | `{"2026-6": {"王小明": 2}}` | 是（key 為 `年-月索引`，月索引 0-11） |
| `skipLeave` | `{"2026-6": {"王小明": true}}` | 是（同上） |
| `closedDays` | `["2026-07-15", ...]` | 否（跨月共用，重置時清空） |
| `blockedDays` | `["2026-07-20", ...]` | 否（同上） |

> ⚠️ `specialIntent`/`skipLeave` 的月份 key 使用 **JS 月份索引（0-11）**，
> 例如 `"2026-6"` 代表 2026 年 7 月。這是歷史設計，改動需同步改讀寫兩端。

---

## 🚀 部署流程

```bash
# 修改後
npm run build          # 本機驗證建置
git add .
git commit -m "描述改動"
git push               # Netlify 自動部署（約 1 分鐘）
```

- GitHub repo：`confitremi-eng/dayoff-scheduler`
- Netlify 會自動注入 `NETLIFY_DATABASE_URL` 環境變數（Neon extension）
- DB schema 由 `db.mjs` 的 `initDB` 自動建立，無需手動 migration

## 🧪 修改後的檢查清單

- [ ] `npm run build` 無錯誤
- [ ] 正職：點日期排休/取消、各項上限提示正常
- [ ] 兼職：點日期選時段（全天/中午/晚上）正常
- [ ] 月份切換：特休/不需排休不跨月殘留
- [ ] 管理後台：密碼 888888 可進入、公休/禁休設定生效
- [ ] 匯出：正職排休表、兼職排班表 CSV 內容正確
- [ ] 多裝置：開兩個視窗，一邊操作另一邊 3 秒內同步
